{
    "name": "github-teams-team",
    "version": "0.1.0",
    "description": "AWS custom resource provider named GitHub::Teams::Team.",
    "private": true,
    "main": "dist/handlers.js",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "npx tsc",
        "watch": "npx tsc -w",
        "prepack": "npm run build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@amazon-web-services-cloudformation/cloudformation-cli-typescript-lib": "^1.0.5",
        "@octokit/rest": "^19.0.5",
        "class-transformer": "0.3.1"
    },
    "devDependencies": {
        "@octokit/types": "^6.34.0",
        "@types/node": "^14.0.0",
        "typescript": "^4.6.4"
    },
    "optionalDependencies": {
        "aws-sdk": "^2.1369.0"
    }
}
