Resources:

  TestRepoSecret:
    Type: GitHub::Repositories::Secret
    Properties:
      Owner: GITHUB_ORG 
      Repository: GITHUB_REPO
      SecretName: secret_name 
      SecretValue: secret-value

# TODO - Make these configurable, replace with AWS owned org